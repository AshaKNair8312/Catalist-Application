<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.catalistapplication">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.Base"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CatalistApplication"
        tools:targetApi="31">
        <activity
            android:name=".views.details.DetailActivity"
            android:exported="false" />
        <activity
            android:name="com.example.catalistapplication.views.home.HomeActivity"
            android:exported="false" />
        <activity
            android:name="com.example.catalistapplication.views.login.LoginActivity"
            android:exported="false" />
        <activity
            android:name="com.example.catalistapplication.views.splash.SplashActivity"
            android:exported="true"
            android:theme="@style/SplashStatusBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>